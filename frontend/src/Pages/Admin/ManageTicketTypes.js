import React from 'react';
import { Button } from '@nextui-org/react';

function ManageTicketTypes({ tickets }) {
	return (
		<>
			<Button color="primary">Add a type</Button>
		</>
	);
}

export default ManageTicketTypes;
